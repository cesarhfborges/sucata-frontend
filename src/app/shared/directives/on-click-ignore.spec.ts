import { OnClickIgnore } from './on-click-ignore';

describe('OnClickIgnore', () => {
  it('should create an instance', () => {
    const directive = new OnClickIgnore();
    expect(directive).toBeTruthy();
  });
});
