import { OnClickClear } from './on-click-clear';

describe('OnClickClear', () => {
  it('should create an instance', () => {
    const directive = new OnClickClear();
    expect(directive).toBeTruthy();
  });
});
